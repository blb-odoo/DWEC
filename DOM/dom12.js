document.addEventListener("DOMContentLoaded", function(event) {
   // Ejemplo textContent
   // document.getElementById("paragraf").textContent = "Nuevo <strong>contenido</strong>";
   // Ejemplo innerHTML
   document.getElementById("paragraf").innerHTML = "Nuevo <strong>contenido</strong>";
});
